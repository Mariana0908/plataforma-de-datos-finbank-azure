# Infraestructura como código

Esta carpeta contiene la infraestructura de Microsoft Azure definida mediante
Terraform.

## Recursos previstos

- Resource Group;
- Azure SQL Database;
- Storage Account con ADLS Gen2;
- contenedores Bronze, Silver y Gold;
- Azure Data Factory;
- Azure Databricks;
- Azure Key Vault;
- Log Analytics Workspace;
- Action Group;

La configuración soportará los ambientes `dev` y `prod` y utilizará un backend
remoto

> Estado: pendiente de implementación.